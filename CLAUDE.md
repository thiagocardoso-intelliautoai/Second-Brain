# Claude Code Instructions - Second Brain

@.claude/CLAUDE.md

This root file exists so Claude Code can load the Second Brain project memory from the vault root.

The imported `.claude/CLAUDE.md` imports `../AGENTS.md`. If an environment does not resolve recursive imports, read `AGENTS.md` manually before doing substantial work.

